[pytest]
asyncio_mode = auto
testpaths = tests
addopts = -m "not live and not migrations"
markers =
    live: needs the full stack on localhost:8000 (./start.sh); run with: pytest -m live
    migrations: needs a pgvector Postgres only (throwaway DBs via MIGRATIONS_ADMIN_DSN); run with: pytest -m migrations
