"""Obsidian integration (ADR-010 adapter; State Engine slice 1 source).

client.py — vault filesystem transport: walk, parse, and the ONLY vault writer
(write_managed, jailed to the managed folder). adapter.py — the ADR-010 seam.
"""
