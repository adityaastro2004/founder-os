# ── Secrets ──────────────────────────
.env
.env.local
.env.*.local
.env.production
.env.*
!.env.example
!.env.*.example
*.pem

# ── Python ───────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.venv/
venv/
*.egg-info/
dist/
build/

# ── Logs ─────────────────────────────
*.log
nohup.out
logs/

# ── OS ───────────────────────────────
.DS_Store
Thumbs.db

# ── IDE ──────────────────────────────
.vscode/
.idea/
*.swp
*.swo

# ── Claude / AI tools ────────────────
# Settings stay local (machine-specific permissions); the eng-* subagent
# definitions are tracked — CLAUDE.md §7 makes them the mandatory workflow and
# §10 links to them, so a fresh clone must have them.
.claude/*
!.claude/agents/
.claude/agents/*
!.claude/agents/eng-*.md

# ── Node ─────────────────────────────
node_modules/
.turbo/
.next/
out/

# ── Backups ──────────────────────────
*.bak
*.backup
.vercel
.env*
!.env*.example

# Auto-generated project context snapshot (stack/git); regenerated on demand.
docs/context.md
